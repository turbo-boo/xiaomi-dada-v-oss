obj-$(CONFIG_XM_POWER_SUPPLY) += mca_strategy_class.o
obj-$(CONFIG_XM_POWER_SUPPLY) += strategy_fg/

# Buck/PD-auth/smart-charge policy is restored after the battery business
# path is validated against Dada userspace ABI.
