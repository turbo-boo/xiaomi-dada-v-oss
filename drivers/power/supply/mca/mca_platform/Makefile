obj-$(CONFIG_XM_POWER_SUPPLY) += mca_platform_cp_class.o
obj-$(CONFIG_XM_POWER_SUPPLY) += mca_platform_fg_ic_ops.o

# Buck charger, BC1.2, load-switch and wireless classes are restored in
# follow-up commits once their Dada DT consumers are connected.
